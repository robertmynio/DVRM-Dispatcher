package vdrm.pred.miner;

import java.util.ArrayList;
import java.util.UUID;

public class MinerTest {

	/**
	 * @param args
	 */
	public static void main(String[] args) {
		// TODO Auto-generated method stub
		IPatternMiner miner = new TreePatternMiner();
		/*
		UUID x = UUID.randomUUID();
		miner.addElement(x);
		x = 2;
		miner.addElement(x);
		x = 1;
		miner.addElement(x);
		x = 3;
		miner.addElement(x);
		x = 2;
		miner.addElement(x);
		x = 1;
		miner.addElement(x);
		x = 3;
		miner.addElement(x);
		x = 1;
		miner.addElement(x);
		x = 2;
		miner.addElement(x);
		x = 1;
		miner.addElement(x);
		
		ArrayList<Byte> pattern;
		x=1;
		pattern = miner.getPattern(x, 0.6);
		if(pattern!=null)
			System.out.println(pattern.toString());
		else
			System.out.println("NULL");
		x=1;
		pattern = miner.getPattern(x, 0.4);
		if(pattern!=null)
			System.out.println(pattern.toString());
		else
			System.out.println("NULL");
		x=2;
		pattern = miner.getPattern(x, 0.5);
		if(pattern!=null)
			System.out.println(pattern.toString());
		else
			System.out.println("NULL");
		x=2;
		pattern = miner.getPattern(x, 0.8);
		if(pattern!=null)
			System.out.println(pattern.toString());
		else
			System.out.println("NULL");
		x=3;
		pattern = miner.getPattern(x, 0.51);
		if(pattern!=null)
			System.out.println(pattern.toString());
		else
			System.out.println("NULL");
		x=3;
		pattern = miner.getPattern(x, 0.6);
		if(pattern!=null)
			System.out.println(pattern.toString());
		else
			System.out.println("NULL");*/
	}

}
